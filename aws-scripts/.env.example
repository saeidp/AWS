# Template. Copy to .env and fill in the values for your organisation:
#     cp .env.example .env
# .env is git-ignored; this file is not, so keep it free of real values.

# Tag namespace used by the account-owner scripts:
#   OWNER_TAG_PREFIX=acme  ->  acme:primary-contact-owner-email
OWNER_TAG_PREFIX=org
