[project]
name = "aws-scripts"
version = "0.1.0"
description = "Operational scripts for AWS Organizations tag compliance and account ownership"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
